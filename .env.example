# Only needed when the static frontend is hosted separately from the Socket.IO server.
# Example: GitHub Pages frontend + Render Socket.IO server.
VITE_SOCKET_URL=https://your-render-service.onrender.com
